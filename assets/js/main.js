document.addEventListener('DOMContentLoaded',()=>{const btn=document.querySelector('.menu-open');const nav=document.querySelector('.nav');const menu=document.querySelector('.menu');const submenuLinks=document.querySelectorAll('.has-submenu > .nav-link.title');if(!btn||!nav)return;const focusSelector='a, button, input, select, textarea, [tabindex]:not([tabindex="-1"])';const getFocusables=()=>nav.querySelectorAll(focusSelector);function setFocusable(enabled){if('inert' in nav)return;getFocusables().forEach(el=>{if(enabled){const prev=el.getAttribute('data-prev-tabindex');if(prev!==null){el.setAttribute('tabindex',prev);el.removeAttribute('data-prev-tabindex')}else{el.removeAttribute('tabindex')}}else{const current=el.getAttribute('tabindex');if(current!==null)el.setAttribute('data-prev-tabindex',current);el.setAttribute('tabindex','-1')}})}
function setMenuState(open){nav.classList.toggle('active',open);btn.setAttribute('aria-expanded',open?'true':'false');nav.setAttribute('aria-hidden',open?'false':'true');if('inert' in nav){if(open)nav.removeAttribute('inert');else nav.setAttribute('inert','')}else{setFocusable(open)}
if(open){const firstLink=nav.querySelector('a, button, [tabindex]:not([tabindex="-1"])');if(firstLink)firstLink.focus({preventScroll:!0})}else{btn.focus({preventScroll:!0})}}
setMenuState(!1);btn.addEventListener('click',(e)=>{e.stopPropagation();const isOpen=nav.classList.contains('active');const willOpen=!isOpen;if(!willOpen){resetSubmenus()}
setMenuState(willOpen)});submenuLinks.forEach(link=>{link.addEventListener('click',(e)=>{e.preventDefault();e.stopPropagation();const parent=link.closest('.has-submenu');const wasActive=parent.classList.contains('active');resetSubmenus();if(!wasActive){parent.classList.add('active');if(menu)menu.classList.add('submenu-open');nav.classList.add('submenu-open');if(link.dataset.original){link.textContent='Voltar'}}})});document.addEventListener('click',(e)=>{if(!e.target.closest('.menu')&&!e.target.closest('.menu-open')){resetSubmenus();setMenuState(!1)}});document.addEventListener('keydown',(e)=>{if(e.key==='Escape'&&nav.classList.contains('active')){resetSubmenus();setMenuState(!1)}});function resetSubmenus(){document.querySelectorAll('.has-submenu').forEach(item=>{item.classList.remove('active');const link=item.querySelector('.nav-link.title');if(link&&link.dataset.original){link.textContent=link.dataset.original}});if(menu)menu.classList.remove('submenu-open');nav.classList.remove('submenu-open')}});document.addEventListener("DOMContentLoaded",()=>{const header=document.querySelector(".header");const sections=document.querySelectorAll("section");if(!header||!sections.length)return;const HEADER_HEIGHT=80;function setHeaderTheme(theme){header.classList.remove("header--dark","header--light");header.classList.add(theme==="dark"?"header--dark":"header--light")}
function onScrollHeaderBg(){if(window.scrollY>50){header.classList.add("header-is-visible")}else{header.classList.remove("header-is-visible")}}
window.addEventListener("scroll",onScrollHeaderBg,{passive:!0});onScrollHeaderBg();const observerOptions={root:null,rootMargin:`-${HEADER_HEIGHT}px 0px -80% 0px`,threshold:0};const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting){const isDark=entry.target.classList.contains("is-dark");setHeaderTheme(isDark?"light":"dark")}})},observerOptions);sections.forEach((section)=>observer.observe(section));function checkInitialTheme(){const headerBottom=header.getBoundingClientRect().bottom;sections.forEach(sec=>{const rect=sec.getBoundingClientRect();if(rect.top<=headerBottom&&rect.bottom>=headerBottom){const isDark=sec.classList.contains("is-dark");setHeaderTheme(isDark?"light":"dark")}})}
checkInitialTheme()});document.addEventListener("DOMContentLoaded",()=>{const floatCta=document.querySelector(".float-cta");const sections=document.querySelectorAll("section");if(!floatCta||!sections.length)return;const SHOW_AFTER=120;const CTA_OFFSET=90;function setFloatCtaTheme(theme){floatCta.classList.remove("float-cta--dark","float-cta--light");floatCta.classList.add(theme==="dark"?"float-cta--dark":"float-cta--light")}
function onScrollFloatCta(){if(window.scrollY>SHOW_AFTER){floatCta.classList.add("float-cta-is-visible")}else{floatCta.classList.remove("float-cta-is-visible")}}
window.addEventListener("scroll",onScrollFloatCta,{passive:!0});onScrollFloatCta();const observerOptions={root:null,rootMargin:`-70% 0px -${CTA_OFFSET}px 0px`,threshold:0};const observer=new IntersectionObserver((entries)=>{entries.forEach((entry)=>{if(entry.isIntersecting){const isDark=entry.target.classList.contains("is-dark");setFloatCtaTheme(isDark?"light":"dark")}})},observerOptions);sections.forEach((section)=>observer.observe(section));function checkInitialTheme(){const checkPoint=window.innerHeight-CTA_OFFSET;sections.forEach((sec)=>{const rect=sec.getBoundingClientRect();if(rect.top<=checkPoint&&rect.bottom>=checkPoint){const isDark=sec.classList.contains("is-dark");setFloatCtaTheme(isDark?"light":"dark")}})}
checkInitialTheme()});document.addEventListener('DOMContentLoaded',()=>{const header=document.querySelector('.header');const offset=header?-header.offsetHeight:0;document.querySelectorAll('a[href*="#"]:not([href="#"])').forEach(link=>{link.addEventListener('click',function(e){if(location.pathname.replace(/^\//,'')===this.pathname.replace(/^\//,'')&&location.hostname===this.hostname){let target=document.querySelector(this.hash)||document.querySelector(`[name="${this.hash.slice(1)}"]`);if(target){e.preventDefault();const top=target.getBoundingClientRect().top+window.pageYOffset+offset;window.scrollTo({top:top,behavior:'smooth'})}}})})});document.addEventListener('DOMContentLoaded',()=>{const items=document.querySelectorAll('.anime');let lastY=window.scrollY;let dir='down';items.forEach(el=>{const rect=el.getBoundingClientRect();if(rect.top<window.innerHeight&&rect.bottom>0){el.classList.add('come-in');el.dataset.initial='true'}});window.addEventListener('scroll',()=>{const y=window.scrollY;dir=y>lastY?'down':'up';lastY=y;if(y<=5){items.forEach(el=>{if(!el.dataset.initial){el.classList.remove('come-in')}})}},{passive:!0});const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{const el=entry.target;if(entry.isIntersecting&&dir==='down'&&!el.classList.contains('come-in')){el.classList.add('come-in')}})},{threshold:0.15});items.forEach(el=>observer.observe(el))});document.addEventListener('DOMContentLoaded',()=>{const items=document.querySelectorAll('.parallax');let lastScrollY=window.scrollY;items.forEach(el=>{el.dataset.scale=1.05;el.style.transform='scale(1.05)'});const clamp=(num,min,max)=>Math.min(Math.max(num,min),max);const onScroll=()=>{const currentScrollY=window.scrollY;const direction=currentScrollY>lastScrollY?'down':'up';items.forEach(el=>{const rect=el.getBoundingClientRect();if(rect.bottom>0&&rect.top<window.innerHeight){let scale=parseFloat(el.dataset.scale);if(direction==='down'){scale-=0.005}else{scale+=0.005}
scale=clamp(scale,1,1.05);el.dataset.scale=scale;el.style.transform=`scale(${scale})`}});lastScrollY=currentScrollY};window.addEventListener('scroll',onScroll,{passive:!0})});document.addEventListener('DOMContentLoaded',()=>{const container=document.getElementById('section-trigger');const videoWrap=document.getElementById('moving-video');const target=document.getElementById('target-slot');const textReveal=document.getElementById('text-reveal');if(!container||!videoWrap||!target||!textReveal)return;const clamp=(value,min,max)=>Math.min(Math.max(value,min),max);const mapRange=(value,inMin,inMax,outMin,outMax)=>{const progress=clamp((value-inMin)/(inMax-inMin),0,1);return outMin+(outMax-outMin)*progress};const easeOutCubic=(t)=>1-Math.pow(1-t,3);const easeInOutCubic=(t)=>{return t<0.5?4*t*t*t:1-Math.pow(-2*t+2,3)/2};function updateVideoPosition(){const rect=container.getBoundingClientRect();const totalScroll=rect.height-window.innerHeight;let progress=-rect.top/totalScroll;progress=clamp(progress,0,1);const moveProgress=easeInOutCubic(mapRange(progress,0.32,0.78,0,1));const textProgress=easeOutCubic(mapRange(progress,0.38,0.78,0,1));textReveal.style.opacity=textProgress;textReveal.style.transform=`translateY(${(1 - textProgress) * 40}px)`;const targetRect=target.getBoundingClientRect();const centerX=window.innerWidth/2;const centerY=window.innerHeight/2;const destX=targetRect.left+targetRect.width/2;const destY=targetRect.top+targetRect.height/2;const initialWidth=Math.min(860,window.innerWidth*0.9);const finalScale=targetRect.width/initialWidth;const currentScale=1-((1-finalScale)*moveProgress);const moveX=(destX-centerX)*moveProgress;const moveY=(destY-centerY)*moveProgress;const radius=12*moveProgress;videoWrap.style.transform=`
      translate(
        calc(-50% + ${moveX}px),
        calc(-50% + ${moveY}px)
      )
      scale(${currentScale})
    `;videoWrap.style.borderRadius=`${radius}px`}
let ticking=!1;function requestTick(){if(!ticking){window.requestAnimationFrame(()=>{updateVideoPosition();ticking=!1});ticking=!0}}
window.addEventListener('scroll',requestTick,{passive:!0});window.addEventListener('resize',updateVideoPosition);window.addEventListener('load',updateVideoPosition);updateVideoPosition()});document.addEventListener('DOMContentLoaded',()=>{const el=document.querySelector('.parallax-video');if(!el)return;let active=!1;const io=new IntersectionObserver(([entry])=>{active=entry.isIntersecting},{threshold:0.01});io.observe(el);let lastScrollY=window.scrollY;let scale=1;const minScale=0.9;const maxScale=1.15;const speed=0.0007;let ticking=!1;function onScroll(){const currentScrollY=window.scrollY;if(!active){lastScrollY=currentScrollY;ticking=!1;return}
const delta=currentScrollY-lastScrollY;scale-=delta*speed;scale=Math.min(Math.max(scale,minScale),maxScale);el.style.transform=`scale(${scale})`;lastScrollY=currentScrollY;ticking=!1}
window.addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(onScroll);ticking=!0}},{passive:!0})});document.addEventListener('DOMContentLoaded',async()=>{const elements=document.querySelectorAll('.lines');if(!elements.length)return;let lastY=window.scrollY;let dir='down';function isInViewport(el){const rect=el.getBoundingClientRect();return rect.top<window.innerHeight&&rect.bottom>0}
function finishAnimation(el){const lines=el.querySelectorAll('.line');const totalTime=600+((lines.length-1)*150)+150;clearTimeout(el._linesTimer);el._linesTimer=setTimeout(()=>{el.classList.add('animation-finished')},totalTime)}
function splitLinesReal(el){if(!el.dataset.originalText){el.dataset.originalText=(el.textContent||'').trim().replace(/\s+/g,' ')}
const text=el.dataset.originalText;if(!text)return;el.classList.remove('animation-finished');el.innerHTML='';const words=text.split(' ');const wordSpans=words.map((w,i)=>{const s=document.createElement('span');s.className='word';s.textContent=w+(i<words.length-1?' ':'');el.appendChild(s);return s});const lines=[];let currentTop=null;let current=[];wordSpans.forEach(s=>{const top=s.offsetTop;if(currentTop===null)currentTop=top;if(top!==currentTop){lines.push(current);current=[];currentTop=top}
current.push(s.textContent)});if(current.length)lines.push(current);el.innerHTML='';lines.forEach((parts,index)=>{const lineWrap=document.createElement('div');lineWrap.className='line';const span=document.createElement('span');span.textContent=parts.join('').trimEnd();span.style.transitionDelay=`${index * 0.15}s`;lineWrap.appendChild(span);el.appendChild(lineWrap)});el.dataset.animated='true';if(el.classList.contains('is-visible')){finishAnimation(el)}}
if(document.fonts?.ready){await document.fonts.ready}
elements.forEach(splitLinesReal);elements.forEach(el=>{if(isInViewport(el)){el.classList.add('is-visible');el.dataset.initial='true';finishAnimation(el)}});window.addEventListener('scroll',()=>{const y=window.scrollY;dir=y>lastY?'down':'up';lastY=y;if(y<=10){elements.forEach(el=>{if(!el.dataset.initial){el.classList.remove('is-visible');el.classList.remove('animation-finished')}})}},{passive:!0});elements.forEach(el=>{const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting&&dir==='down'){el.classList.add('is-visible');finishAnimation(el)}})},{threshold:0.6});observer.observe(el)});let t=null;window.addEventListener('resize',()=>{clearTimeout(t);t=setTimeout(()=>{elements.forEach(splitLinesReal)},150)},{passive:!0})});document.addEventListener("DOMContentLoaded",()=>{const intensity=200;const reduceMotion=window.matchMedia?.("(prefers-reduced-motion: reduce)")?.matches;if(reduceMotion)return;const items=Array.from(document.querySelectorAll(".scroll-parallax")).map((section)=>({section,left:section.querySelector(".direita"),right:section.querySelector(".esquerda"),}));if(!items.length)return;items.forEach(({left,right})=>{if(left)left.style.willChange="transform";if(right)right.style.willChange="transform"});let ticking=!1;const update=()=>{ticking=!1;const vh=window.innerHeight;for(const{section,left,right}of items){const rect=section.getBoundingClientRect();if(rect.bottom<=0||rect.top>=vh)continue;const progress=(vh-rect.top)/(vh+rect.height);const clamped=Math.max(0,Math.min(1,progress));const move=intensity*(1-clamped);if(left)left.style.transform=`translate3d(${-move}px, 0, 0)`;if(right)right.style.transform=`translate3d(${move}px, 0, 0)`}};const onScroll=()=>{if(ticking)return;ticking=!0;requestAnimationFrame(update)};const onResize=()=>onScroll();window.addEventListener("scroll",onScroll,{passive:!0});window.addEventListener("resize",onResize,{passive:!0});onScroll()});document.addEventListener('DOMContentLoaded',()=>{if(window.innerWidth<991)return;const section=document.querySelector('.destaques-parallax');const items=section?.querySelectorAll('.parallax-item');if(!section||!items.length)return;const totalItems=items.length;const holdFactor=0.25;const baseScroll=window.innerHeight*totalItems;const scrollLength=baseScroll*(1+holdFactor);const spacer=document.createElement('div');spacer.className='parallax-spacer';spacer.style.height=`${scrollLength}px`;section.after(spacer);const sectionTop=section.offsetTop;function clamp(v,min=0,max=1){return Math.max(min,Math.min(max,v))}
function onScroll(){const scrollY=window.scrollY;const raw=(scrollY-sectionTop)/scrollLength;const animProgress=clamp(raw/(1/(1+holdFactor)));items.forEach((item,index)=>{const start=index/totalItems;const end=(index+1)/totalItems;let itemProgress=(animProgress-start)/(end-start);itemProgress=clamp(itemProgress);const eased=1-Math.pow(1-itemProgress,3);const translateX=90*(1-eased);item.style.transform=`translateX(${translateX}%)`;item.style.opacity=0.4+(0.9*eased)})}
window.addEventListener('scroll',onScroll,{passive:!0});onScroll()});document.addEventListener('DOMContentLoaded',function(){let slug=window.location.pathname.replace(/^\/|\/$/g,'')||'home';const links=document.querySelectorAll('a[href*="/orcamento"]');links.forEach(link=>{let url=new URL(link.href,window.location.origin);if(!url.searchParams.has('origem')){url.searchParams.set('origem',slug);link.href=url.toString()}})});(()=>{const cards=document.querySelectorAll('.glow-card');cards.forEach((card)=>{let raf=0;let lastX=0,lastY=0;function setVars(){raf=0;card.style.setProperty('--mx',`${lastX}px`);card.style.setProperty('--my',`${lastY}px`)}
card.addEventListener('pointermove',(e)=>{const r=card.getBoundingClientRect();lastX=e.clientX-r.left;lastY=e.clientY-r.top;if(!raf)raf=requestAnimationFrame(setVars)});card.addEventListener('pointerenter',()=>{card.style.setProperty('--alpha','1')});card.addEventListener('pointerleave',()=>{card.style.setProperty('--alpha','0');card.style.setProperty('--mx',`50%`);card.style.setProperty('--my',`50%`)})})})();document.addEventListener('DOMContentLoaded',()=>{const counters=document.querySelectorAll('.counter');const animateCounter=(counter)=>{const match=counter.textContent.match(/[\d,.]+/);if(!match)return;const target=parseFloat(match[0].replace(',','.'));const duration=1500;const frameRate=30;const totalFrames=Math.round(duration/(1000/frameRate));let frame=0;const prefix=counter.textContent.split(match[0])[0];const suffix=counter.textContent.split(match[0])[1];const counterInterval=setInterval(()=>{frame++;const progress=Math.min(frame/totalFrames,1);const easedProgress=progress*(2-progress);const current=Math.round(target*easedProgress);counter.textContent=`${prefix}${current.toLocaleString('pt-BR')}${suffix}`;if(progress>=1){clearInterval(counterInterval);counter.textContent=`${prefix}${target.toLocaleString('pt-BR')}${suffix}`}},1000/frameRate)};const observer=new IntersectionObserver((entries,obs)=>{entries.forEach(entry=>{if(entry.isIntersecting){animateCounter(entry.target);obs.unobserve(entry.target)}})},{threshold:0.3});counters.forEach(counter=>observer.observe(counter))})